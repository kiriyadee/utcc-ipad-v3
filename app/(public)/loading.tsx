"use client";

import UTCCLoadingIcon from "@/components/loading-icon";

export default function Loading() {
  return (
    <div className="flex items-center content-center w-full h-screen relative">
      <UTCCLoadingIcon />
    </div>
  );
}
